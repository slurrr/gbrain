# Current Goal
Run Alex as a custom Pi workspace using gbrain's memory-only integration. Alex's authored identity stays in the workspace; gbrain is the sole memory system. Pi remains the harness and AgentMux's local model is gbrain's backstop. No parallel filesystem memory, transcript capture, or bootstrap-generated agent body.

# Current State
- Workspace: `/home/poop/Alex-workspace`.
- Current commits: Alex workspace `25d0732 launcher: disable context file crawl` (with `fb71801 fix: avoid false AgentMux fallback` immediately before it); gbrain fork `55209e2c1 docs: record gbrain error propagation`. The PATH-facing `/home/poop/.local/bin/alex` now delegates to the repo-local policy launcher; Alex has a user-session modification in `brain/projects/pi-collab.md`, intentionally not staged.
- Removed legacy `MEMORY.md` and `memory/`; these were Alex's old filesystem-memory layer, not gbrain components. `ideas/` and authored identity files remain preserved.
- `.gbrain-source` pins the workspace source to `workspace`; `gbrain sources current --json` resolves `source_id: workspace`.
- `.pi/extensions/gbrain.ts` registers the full native memory/page/timeline/graph bridge, while `.pi/alex-policy.json` exposes only read/search/edit/write plus Alex's six interactive memory tools: recall, remember, entity, forget, get_page, and put_page. The bridge reapplies the allowlist after registration and before each turn; ambient context plumbing remains internal. `bin/alex` launches Pi with `--no-skills` and adds only the 17 explicit `.pi/alex-policy.json` skill paths. Fresh session and post-compaction boundary packs now default to standing entities `alex,seth`; `GBRAIN_CONTEXT_ENTITIES` overrides that default.
- All bridge calls use the native `gbrain call --source workspace` operation surface, run from the workspace root, and pass `OPENAI_BASE_URL=http://127.0.0.1:8002/v1`, local `OPENAI_API_KEY=dummy`, and `--quiet`. Page, fact, timeline, and link writes perform read-back verification.
- Ambient retrieval follows gbrain placement: confidence-gated `volunteer_context` per non-command turn; `context_pack` at session start and post-compaction; a session-cursor `delta` wake every eight turns. Full hybrid `recall` is explicit only. Trusted-local boundary/delta packs include private facts by deliberate creative-workspace policy. The rolling session window is used ephemerally for entity detection; no transcript archive or extraction is enabled.
- Creative-memory policy is project/collaboration/problem-solving focused: Alex's thinking preferences, how Alex and Seth work together, durable project facts, decisions/rationale, reusable workflows, and lessons. Schedules, email, errands, raw transcripts, ordinary brainstorm fragments, and unconfirmed speculation are out of scope. Remember visibility defaults explicitly to `world` so useful context is not filtered out.
- `.pi/extensions/agentmux.ts` discovers `/v1/models` at `http://127.0.0.1:8002/v1`, syncs the first advertised server id into gbrain `models.default`, and overlays only AgentMux endpoint/auth/compat settings in Pi. It deliberately does NOT provide a `models` array, preserving the curated `~/.pi/agent/models.json` local aliases. Workspace default is `agentmux/qw3.8-exl3`; `/alex-model` selects that alias. Pi-only fallback is `openai-codex/gpt-5.6-luna` at `xhigh`; gbrain-native work does not use that fallback.
- Pi extension commands load; `/alex prompt stats/json` showed the authored prompt stack in the intended order. With the endpoint serving `qw3.8-3.67-direct`, Pi's stable `agentmux/qw3.8-exl3` alias successfully completed a real `OK` request and the response reported the canonical server model. `pi --list-models` shows all eight curated AgentMux aliases plus Codex models. Bun builds and `git diff --check` pass. LSP still reports missing workspace dependencies/types. Skill gating was verified end-to-end: direct `pi --no-skills` reported no skills, while the PATH-facing `alex` command reported exactly the 17 policy entries. Gbrain tool failures now go through a shared `requireGbrainResult` helper that throws on nonzero exits and failed read-back verification; real nonzero and simulated verification failures both produced Pi `isError: true` tool results. Periodic delta now runs from `agent_settled`, not `turn_end`, so one agent request with two tool executions produced exactly one delta wake after eight prompts. AgentMux fallback detection now checks the structured last assistant `stopReason` instead of regex-scanning all message text; a real read-of-the-link-skill turn completed locally without a false Luna fallback.
- Runtime state: installed `gbrain` was upgraded to 0.46.30.0 and schema migration 141 applied; AgentMux port 8002 is serving `qw3.8-3.67-direct`; the local embedding/reranker service at port 8766 is reachable. The workspace source was caught up with a scoped `gbrain sync --source workspace` (no reset/reindex/delete); source status is fresh, 2 pages / 2 source chunks / 100% embedded, and the scheduled one-shot sync now runs through the native systemd user units `alex-gbrain-sync.service` + `alex-gbrain-sync.timer` every 5 minutes. After sync, doctor reports `sync_freshness=ok`, `embeddings=ok`, `embedding_provider=ok`, `embed_staleness=ok`, and `reranker_health=ok`. Committed workspace state was pushed to origin; three unrelated user-edited files remain unstaged. `docs/GBRAIN_SKILLS_INDEX.md` now catalogs all 69 routed skills and marks Alex/background/operator fit.

# Decisions
- Treat the current workspace as a custom Pi creative agent with gbrain memory-only integration, not `gbrain bootstrap`; custom identity/runtime ownership is correct.
- Keep no transcript capture/extraction for now. Gbrain's full bootstrap transcript/dream lane is a separate optional capability, not required for the memory-only protocol.
- Preserve native gbrain operations and use the documented ambient placement. The trusted local creative workspace intentionally widens context_pack/delta visibility and defaults ordinary remembered facts to world visibility; this is not a new privacy system.
- Do not expand fact/page verification or slug normalization yet; observe native gbrain behavior in real creative use first.
- Pi model discovery is health/gbrain routing only; the curated models.json catalog owns visible/selectable aliases. A discovered server id is not injected as a replacement model.

# Open Problems
- The source-sync path is now active, but monitor `systemctl --user status alex-gbrain-sync.timer` and `journalctl --user -u alex-gbrain-sync.service`; a dirty workspace can still block a conflicting git pull even though the sync unit never overwrites uncommitted files.
- Validate the new ambient path with a real Pi turn: boundary default `alex,seth` was tested in a fresh `alex --no-session --mode json --print` run; the model received the injected pack and correctly named both entities plus a Seth fact. Still test volunteer pointers, post-compaction behavior, periodic delta, and explicit memory/page/graph tool round-trips. Runtime inspection confirmed the narrowed 12-tool Pi allowlist and launcher skill suppression/explicit loading. Embeddings and reranking are now verified end-to-end: `gbrain models doctor --skip=openai` returned 4/4, doctor reports 100% embedding coverage, and a no-expansion hybrid query returned cosine plus rerank scores. Direct chat/expansion probes still require the AgentMux env that the Alex bridge supplies.
- The remaining doctor warnings about bootstrap receipt/serve/reflex state are old or expected custom-harness residue unless a live lock persists. The investigated session `2026-08-26T19-00-10-942Z_01a03f71-b23e-7be5-9d5b-6ca9258d85e9.jsonl` had a successful local `stopReason: "stop"` response followed by an erroneous fallback because the old detector matched `network`/`timeout` text inside the loaded brain-link skill. Alex's launcher now passes Pi's native `--no-context-files`, disabling all `AGENTS.md`/`CLAUDE.md` discovery while preserving `.pi` identity/resources.
- Dream remains deferred and should be added only after the memory-only path passes a clean round-trip test.

# Resume Instructions
From `/home/poop/Alex-workspace`, use `docs/GBRAIN_SKILLS_INDEX.md` as the portable catalog when choosing skills for another harness. The PATH-facing `alex` command delegates to `bin/alex`; it always passes `--no-skills` and adds only the 17 paths in `.pi/alex-policy.json`. `bin/alex` rejects direct `--skill` bypasses. The current Alex allowlist is read/search/edit/write plus recall, remember, entity, forget, get_page, and put_page. Then test one non-command turn, boundary/compaction behavior, periodic delta, and one approved native page/fact/graph round-trip. Do not initialize, reset, reindex, or delete existing brain data without explicit approval.
